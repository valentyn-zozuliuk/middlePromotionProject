describe('Login page', () => {

    it('should login user', () => {
        expect(true).to.equal(true);
    });

    it('should login user 222', () => {
        expect(true).to.equal(true);
    });
});
